@page "/countries/{Id:guid}"

@inherits ComponentBase
@inject IDialogService DialogService
@using API.Data.DTOs
@using MudBlazor

<MudDialog>
	<DialogContent>
		<MudForm @ref="editForm">
			<MudTextField @bind-Value="country.Name" Label="Name" />
			<MudNumericField @bind-Value="country.Position" Label="Initial Position" />
			<MudNumericField @bind-Value="country.Position" Label="Current Position" />
			<MudNumericField @bind-Value="country.TotalPoints" Label="Total Points" />
			<MudNumericField Value="country.NumberOfInitialTeams" Label="Initial Teams" />
			<MudNumericField Value="country.NumberOfActiveTeams" Label="Active Teams" />
		</MudForm>
	</DialogContent>
	<DialogActions>
		<MudButton Color="Color.Primary" Variant="Variant.Text" OnClick="Cancel">Cancel</MudButton>
		<MudButton Color="Color.Primary" Variant="Variant.Filled" OnClick="Save">Save</MudButton>
	</DialogActions>
</MudDialog>

@code {
	[Parameter] public CountryDto Country { get; set; } = default!;
	[CascadingParameter] IMudDialogInstance MudDialog { get; set; } = default!;

	private MudForm editForm = default!;
	private CountryDto country = new();

	protected override void OnParametersSet()
	{
		country = new CountryDto
		{
			Position = Country.Position,
			Name = Country.Name,
			TotalPoints = Country.TotalPoints
		};
	}

	private void Save()
	{
		var options = new DialogOptions { CloseOnEscapeKey = true };
		MudDialog?.Close(DialogResult.Ok(country));
	}

	private void Cancel()
	{
		MudDialog?.Close(DialogResult.Ok(country));
	}
}